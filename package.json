{
  "name": "intensity-app",
  "version": "1.0.0",
  "description": "",
  "main": "../BE/src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bdev": "cd BE && npm run dev",
    "server": "cd BE && nodemon final/index.js",
    "client": "cd FE && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "deploy": "cd BE && npm run deploy:full"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.9"
  }
}
